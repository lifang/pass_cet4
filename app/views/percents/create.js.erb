<% if @web=="qq" %>
$('.m_box').html("<%= escape_javascript(render :partial => "/percents/result") %>");
<% else %>
$('.m_box').html("<%= escape_javascript(render :partial => "/percents/result") %>");
<% end %>
window.scrollTo(0, 0);
